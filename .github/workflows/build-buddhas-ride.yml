name: Build Buddhas Ride APK

on:
  workflow_dispatch:
  push:
    branches: [ buddhas-ride-app ]
    paths:
      - 'BuddhasRideAndroid/**'
      - '.github/workflows/build-buddhas-ride.yml'

permissions:
  contents: read

jobs:
  build-buddhas-ride:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: BuddhasRideAndroid
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Verify isolated Buddhas Ride Firebase + FCM project
        run: |
          grep -q "applicationId 'com.buddhaspinas.buddhasride'" app/build.gradle
          grep -q "versionName '1.0.2'" app/build.gradle
          grep -q "id 'com.google.gms.google-services'" app/build.gradle
          grep -q 'com.buddhaspinas.buddhasride' app/google-services.json
          grep -q 'requestFcmToken' app/src/main/java/com/buddhaspinas/buddhasride/MainActivity.java
          grep -q 'FirebaseMessagingService' app/src/main/java/com/buddhaspinas/buddhasride/BuddhasRideFirebaseMessagingService.java
          grep -q 'https://rider.buddhaspinas.com/' app/src/main/java/com/buddhaspinas/buddhasride/MainActivity.java
      - name: Build installable debug APK
        run: gradle assembleDebug --stacktrace
      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk "Buddhas Ride v1.0.2 FCM.apk"
      - uses: actions/upload-artifact@v4
        with:
          name: Buddhas-Ride-v1.0.2-FCM-APK
          path: "BuddhasRideAndroid/Buddhas Ride v1.0.2 FCM.apk"
          if-no-files-found: error
