name: Build BuddhasPalm Android APKs

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build-admin:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: BuddhasPalmAdmin
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Build Admin APK
        run: gradle assembleDebug
      - name: Rename Admin APK
        run: cp app/build/outputs/apk/debug/app-debug.apk "Buddhas Admin.apk"
      - uses: actions/upload-artifact@v4
        with:
          name: Buddhas-Admin-APK
          path: "BuddhasPalmAdmin/Buddhas Admin.apk"

  build-provider:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: BuddhasPalmProvider
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Build Provider APK
        run: gradle assembleDebug
      - name: Rename Provider APK
        run: cp app/build/outputs/apk/debug/app-debug.apk "Provider app.apk"
      - uses: actions/upload-artifact@v4
        with:
          name: Provider-app-APK
          path: "BuddhasPalmProvider/Provider app.apk"

  build-hub:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: BuddhasPalmHub
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Build Hub APK
        run: gradle assembleDebug
      - name: Rename Hub APK
        run: cp app/build/outputs/apk/debug/app-debug.apk "Buddhas Palm Hub.apk"
      - uses: actions/upload-artifact@v4
        with:
          name: Buddhas-Palm-Hub-APK
          path: "BuddhasPalmHub/Buddhas Palm Hub.apk"

  build-tutor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Extract BuddhaStudy Tutor source
        run: |
          rm -rf BuddhaStudyTutor
          mkdir -p BuddhaStudyTutor
          unzip -q BuddhaStudyTutor-source.zip -d BuddhaStudyTutor
      - name: Build Tutor APK
        working-directory: BuddhaStudyTutor
        run: gradle assembleDebug
      - name: Rename Tutor APK
        run: cp BuddhaStudyTutor/app/build/outputs/apk/debug/app-debug.apk "BuddhaStudy Tutor.apk"
      - uses: actions/upload-artifact@v4
        with:
          name: BuddhaStudy-Tutor-APK
          path: "BuddhaStudy Tutor.apk"
